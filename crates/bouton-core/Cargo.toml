[package]
name = "bouton-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
evdev = { version = "0.12", optional = true }

[features]
evdev-support = ["evdev"]
